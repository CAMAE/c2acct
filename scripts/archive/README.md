# Archived Scripts Index

This directory contains obsolete or one-off scripts kept for history only.

Rules:
- Nothing here should be treated as a canonical launch, seed, validation, or operator script.
- Prefer active scripts under `scripts/` first.
- Keep historical scripts here only when they still provide forensic value or rollback context.

Current categories:
- `submit-via-api.js`
- `verify-survey-submissions.js`
- `backfill-survey-submissions.js`
- `obsolete/`: superseded repair or migration helpers that should not be reused as current tooling

Canonical current script entrypoints live under:
- `scripts/seed-pat-runtime.ts`
- `scripts/validate-launch.ts`
- `scripts/validate-db.ts`
- `scripts/mac-mini/**`
