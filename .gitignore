# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing / build
/coverage
/.next/
/playwright-report/
/test-results/
/blob-report/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# environment
.env*
.envrc
.direnv/
.vercel

# generated runtime state
/app/generated/prisma
tmp/
.tmp/
tmp-*.sql
tmp-*.json
*.log
logs/
*.tmp
*.temp
*.zip
*.tar
*.tar.gz
*.tgz
artifacts/mac-mini/*
artifacts/mac-mini/logs/
artifacts/mac-mini/reports/
artifacts/mac-mini/state/
artifacts/mac-mini/launchd/
artifacts/reports/
artifacts/agents/
artifacts/audit/
artifacts/launch-proof/
artifacts/release/
artifacts/visual/
# Tenant data-portability exports (contain PII — never commit).
artifacts/tenant-exports/

# local machine noise
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# generated docs and scratch notes
docs/audit/audit_*.md
docs/audit/session_*.md
.auth-*.txt
.next-aae-checkpoint.txt
.rotate-secrets-now.txt
.copilot-*.txt
copilot-*.txt
*_copilot*.txt
payload.json

# agent workspace
agent-work/

# backups / debris
*.bak
*.bak.*
*.bak_*
*.pre_*_backup
*_bak/
**/_bak/**

# Python dev envs (local-only, never committed)
.venv/
.venv311/
myenv/
get-pip.py
generate_text.py

# Claude Code / Cowork session context (local-only notes)
CLAUDE.md

# Brand screenshots staging area (regenerated by ops:mac-mini:screenshots)
artifacts/brand-screenshots/

# Ephemeral review-screenshot capture dirs (per-review-pass; never tracked).
# Keeping these untracked would dirty the tree and block com.c2acct.app startup
# ("Dirty git tree is forbidden for startup").
artifacts/*-shots/

# Block-2 runtime-proof probe scratchpad (Day 16; never committed)
scripts/dev/


# pilot login reset output (plaintext credentials — never commit)
pilot-login-reset.local.md

# Elite-sprint planning/spec docs are now tracked in this private repo (B5-7).
# Secret scan (B5-7) confirmed no keys/tokens/connection strings in these docs.

# Block 9d: rotation-verification proof output (keep dir, ignore proofs)
artifacts/rotations/*
!artifacts/rotations/.gitkeep
