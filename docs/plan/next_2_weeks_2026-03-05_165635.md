# NEXT 2 WEEKS — 2026-03-05 16:56:35

## North Star Demo (end of week 2)
- URL-first multi-tenant flow: select company -> survey -> submit -> results -> outputs (no manual companyId query params)
- Tier1 outputs visible (locked/unlocked) with real insight cards
- Audit loop stays PASS (build/lint/prisma/migrate status)

## Week 1 (Days 1–5): Productize the golden path
- [ ] Add Company context (cookie or session) so /results and /outputs don’t require ?companyId=
- [ ] Add Company selector UI (dropdown) + persist selection
- [ ] Update /survey/[key] submit to use selected company automatically
- [ ] Remove any remaining “companyId required” UX dead-ends
- [ ] Make /api/results default to selected company (fallback: latest company in DB for dev)
- [ ] Add a single “Demo mode” seed command that seeds: module + questions + demo company + tier1 badge/rules + tier1 insights

## Week 2 (Days 6–10): Capability unlock wiring (real unlock engine)
- [ ] Replace Tier1 stub unlock with capability-based unlock using InsightCapabilityRule
- [ ] Seed at least 1 CapabilityNode + ModuleCapability + SurveyQuestionCapability mapping for firm_alignment_v1
- [ ] On submit: compute/store capability scores (already wired) and evaluate unlocks from those scores
- [ ] Outputs page: show locked cards greyed with tooltip + unlocked cards with content
- [ ] Add minimal regression smoke: seed -> submit -> expect (badges>=1, unlockedInsights>=1)

## Guardrails
- [ ] Ensure all seed scripts are idempotent and committed
- [ ] Ensure run_audit.ps1 uses pnpm exec prisma (no npx drift)
- [ ] Keep save PASS; if FAIL, fix root cause before new feature work
