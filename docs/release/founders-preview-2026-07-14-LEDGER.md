# Founders-Preview Deploy — Ledger (patalign.com)

**Re-anchor doc.** Written so a fresh session (post-`/clear`) can resume the prod push
without re-deriving state. Read `CLAUDE.md` (hard rules, validation chain) + this file +
`docs/e2e-known-stale.md` before running `vercel --prod`.

## ⏭ RESUME STATE (2026-07-17, PROD DEPLOYED b6d8124, 4 gates GREEN) — READ FIRST
**Prod is LIVE on patalign.com at `b6d8124` (`releaseId=b6d8124:b6d8124-mroccpdx`, cloud-build).** The 13c-g + 13h + 13i + e2e/audience + 13d membership bundle shipped. Branch `feat/agent-system-phase-0`, HEAD `ec113e48` (code=`b6d8124e`, clean tree).
- **🟢 BUNDLED `--prod` DONE + 4 PROD GATES GREEN (2026-07-17):** deploy `pat-c2acct-live-ad9vl05v8` aliased to patalign.com, bake `commit=b6d8124 buildId=b6d8124-mroccpdx source=cloud-build`.
  1. **fingerprint** ✅ `releaseId=b6d8124:b6d8124-mroccpdx` · commitShort=b6d8124 · buildSourceType=cloud-build.
  2. **/api/health/db** ✅ 200 ok:true.
  3. **prisma-engine assert** ✅ (`--deployed https://patalign.com` → 200 ok=true, query engine reaches DB).
  4. **asset-integrity** ✅ served BUILD_ID `b6d8124-mroccpdx` == fingerprint buildId (5 refs each on /sign-in + /methodology) · **10/10 assets 200+typed** both routes. NB: `asset-integrity-check.mjs` anchors to LOCAL `.next/BUILD_ID`, which never matches a cloud-build prod (different buildId) → it reports FAIL on the "== disk" line; the real criterion (**served == fingerprint, assets 200**) is verified by curl'ing prod HTML for the fingerprint buildId. **Runbook: for a cloud-build prod, gate asset-integrity by served==fingerprint, not the script's local-disk compare.** See [[feedback_asset_integrity_cloud_build_prod]].
- **Founders-preview `b6d8124` closing pass: Mythos on patalign.com** — 13d membership copy + everything else authed (prev prod `e1ebd610`).
- **🟢 BLOCK 14 — FOUNDER FEEDBACK QUICK WINS (ship before July 18) — CODE DONE + :3005-VERIFIED (2026-07-17, commit `768494b6`).** Option-3 cadence; full regression + screenshots + bundled deploy (cloud-build → preview-gate → --prod) at the END.
  - **14a** shared `PortalHeroChips` "← Workspace" back chip on every portal page (firm/vendor/consultant) via the layouts (one hook). **14b** Pro/Elite tier flag (band chip, not a banner; consultants have none). **14c** "Upgrade to Elite" chip — Pro only, NEVER Elite; contract-locked (`tests/portal-hero-chips.contract.test.ts`).
  - **14d** Alignment Assessment "Next Best Step" tile → real `<a>` (resume next module, or all-done → `/firm/insights`). Sweep: BattleCard "Suggested next actions" are advice text, no nav target → left.
  - **14e** vendor card "Product Insight" → "Product Intelligence" (locale.ts + fallback). **14f** BattleCard "Prospect Intelligence" subtitle + evidence-honest explainer above the firm cards.
  - Verified on `:3005` (Elite/Pro/consultant): Elite upgrade=0, Pro upgrade present, tier flags correct, Product Intelligence live, Next Best Step is a link, Prospect Intelligence on BattleCard, 0 TypeErrors. typecheck + eslint clean · **879 unit**.
  - **Rulings (not in scope):** BattleCard stays ONE word; sandbox veiled-toggle SKIPPED; C2Acct metadata + C2X routing = **F15** (post-launch); per-product + competitive battlecards = **F12** (post-launch, first after the 23rd).
  - **✅ PLACEMENT FIXED (Mythos ruling, commit `aff95645`):** the layout-hook row floated blindly + broke at 1216px (Elite chip overlapped hero border, ← Workspace stranded bottom-left). Reverted the layout hooks; chips now render INSIDE the hero card top-right via a shared `HeroChips` (async wrapper) + `HeroChipsView` (pure, absolutely positioned, corner order `[← Workspace][tier][Upgrade if Pro]`, unit-tested). Rendered in the hero of every navigable surface (firm/vendor/consultant home, `InsightsModeShell` for insights, BattleCard via a `heroChips` slot into the client, assessment/product/admin/profile/catalog pages). **Verified on :3005 at 1216 AND 1600** (5 Mythos pages): chips inside the hero card, top-right, 0 page errors — screenshots in `/tmp/block14-shots`. **Deferred:** alignment-board flag-on + deep consultant ecosystem sub-pages + flag-dark individual pages.
  - **✅ Mythos sign-off (in-browser, vendor home + battlecard): Block 14 build APPROVED.**
  - **✅ FULL REGRESSION GREEN (2026-07-17, `validate:launch` VL_EXIT=0):** lint · validate:db · typecheck · **unit 879** · build · release:prelaunch · promote-known-good · **e2e:local-review 26/1-skip/0-fail** · e2e:release-integrity 2 · PASS. NB: caught 2 STALE 13d e2e assertions (`expectScaffoldCheckout` looked for the removed "checkout scaffold" heading) — realigned to `/membership checkout/i` (commit `eb6bfb6`, test-only; 13d shipped without full e2e so it surfaced now). NOT a Block 14 defect.
  - **✅ PREVIEW GATE GREEN:** cloud-build preview `pat-c2acct-live-g08vufgmo` — status Ready, baked `eb6bfb6:eb6bfb6-mroeqz28` source=cloud-build (pinned to HEAD `eb6bfb6b`, no chimera).
  - **🟢 NEXT: Cam fires the bundled `--prod` of HEAD `eb6bfb6b`; then I run the 4 prod gates; then Mythos closing pass on patalign.com.** Prev prod `b6d8124`. No prod DB work owed (pure code). Commands:
    ```
    git tag -f founders-preview-2026-07-13 eb6bfb6b
    env -u AI_AGENT -u CLAUDECODE vercel --prod \
      --build-env PAT_COMMIT_SHA=eb6bfb6bd6dcc32ccdc1082616eb47f58518995c \
      --build-env PAT_COMMIT_REF=feat/agent-system-phase-0 \
      --build-env PAT_BUILD_SOURCE=cloud-build
    ```
- **Preview gate (pre-prod, GREEN):** cloud-build preview `pat-c2acct-live-b2dpctt0x` — Ready, baked `b6d8124:b6d8124-mrobyiak`. Mythos SSO-browser-verified the preview fingerprint before `--prod`.
- **Local review env:** `:3005` built standalone up on `6495bb4` (docs-identical to HEAD); ELITE accounts + both boosted vendors seeded. Direct Bridgepath login: `demo-vendor-bridgepath@pat.local / PatVendorBridgepath7x` (ELITE). Restart after DB reset: rebuild → promote-known-good → `node .next/standalone/server.js` PORT=3005 + flags; re-seed via demo-expand→compute→swaps→preview-pat-setup.
- **Prod is LIVE on patalign.com at `e1ebd610`.** (Historical detail below.) Block 13 hardening context (Mythos 5-account sweep verdict). Branch `feat/agent-system-phase-0`.
- **Deployed to prod:** HEAD **`a3e8e2af`** → `releaseId=a3e8e2a:a3e8e2a-mrmsi679` (health 200, asset-integrity PASS, 13a walls intact). **`PAT_ENABLE_PAT_ASSISTANT` + `PAT_ENABLE_PINGS` now BOUND** (redeploy 2026-07-16T00:47Z; `/notifications` route live).
- **DONE:** 13a role wall + 13b diagnostics leak (deployed, 867 unit green). 13j `compute:benchmarks` run on prod (Peer Position cohort live).
- **13k Meridian — ✅ CLOSED (Mythos browser + inspector matched, 2026-07-16):**
  - **Demand Signals ✅** — 12 in / 5 out / net +7.
  - **BattleCard ✅** — prod **`strong=4 good=2 weak=12`** (all 3 lanes; vendorStrength=59, good-lane deltas 5.7/2.2 in the 0-12 band). Fix history: run1 `strong=0` → lower-all `strong=4 good=0` (overshot) → **3-lane roster (bc287456):** strong=struggling lowered 25pts, good=tight `[50,56]`, weak=mid/compliance.
  - **Category ◑ Option A** — 4/5, Payroll suppressed (vendor-count-limited), principled. **Suppressed copy → 13c.**
  - **A3 reconcile:** inspector (`getVendorBattleCardData`+`fitTierKey`) and surface (`VendorBattleCardClient`) use IDENTICAL logic; page is `force-dynamic`, no cache. The earlier inspector/surface mismatch was **timing** (local-inspector vs prod-surface-old-boost), NOT a reader split. Reconciled.
  - **Category — Option A (Mythos):** renders 4/5; Payroll (4 vendors) + Audit (<5) honest-suppressed. `n` = **vendor count** (≥5). **Suppressed copy → 13c** (reads as error + wrongly says "firms", is "vendors").
  - Consent seeded for 5 `@c2acct.com` review accounts (Ask Pat bar). **Authed verify (Ask Pat + 3 surfaces) = MYTHOS SWEEP.**
- **Flags:** parity table in Block-13 §. Law: **flag parity = rendered surfaces, not env presence.**
- **✅ P0 Ask Pat — RESOLVED (Mythos browser GREEN 2026-07-16; FOUNDERS EMAIL SENT):**
  - **P0a (backend down):** prod Vercel `ANTHROPIC_API_KEY` bound to the **stale June zero-balance key** (`.env.prod` had the funded one, curl 200). Fix = **key swap** (`vercel env rm`+re-add from `.env.prod`) + **redeploy `e1ebd610`**.
  - **P0b (empty library):** audience + retrieval healthy (vendor→5 chunks); the term **"alignment delta" was absent from the help corpus** (UI-only in `VendorBattleCardClient`) → model declined via `INSUFFICIENT_CONTEXT`. Fix = **content**: defined it in `help/vendor/battlecard.md` (commit `3d432d2b`) + **re-index on prod** (`scripts/index-help.ts`) — DB-only, **NO redeploy** (retrieval reads live). Confirmed: top chunk battlecard.md, `insufficientContext=false`, grounded answer + sources.
  - Diagnostics kept: `scripts/demo/diagnose-help-retrieval.ts` (corpus/tsv/roleAccess), `diagnose-pat-audience.ts` (audience→retrieve→model end-to-end).
- **🟢 DEPLOYED PROD = `e1ebd610`** (Ask Pat key). **Branch HEAD = `3d432d2b`** (P0b content + diagnostics) — DB-only, deployed-app-equivalent, no redeploy owed.
- **✅ BRIDGEPATH boost — PROD GREEN + CLOSED (2026-07-16), all three surfaces ✅.** New bench-variant `scripts/demo/boost-vendor-bench-firms.ts` (commit `dde71d52`; sources `loadVendorCatalog`/`buildVendorPlan` — the `planEcosystems()` bank — NOT `loadExpansionBank`; canonical `demo-bench-firm-` prefix + firmIndex offset 60; vendorIndex=catalog idx=1). **Key improvement over the Meridian expansion boost: lane bands are vs-RELATIVE** — the script measures the vendor's live `vendorStrength` up front and derives each lane center from it (strong=vs-26, good=vs-6, weak=vs+8), because Bridgepath's vs≈68 would make Meridian's hard-coded good-band [50,56] overshoot to STRONG (68-53=15).
  - **PROD run (Neon `ep-hidden-dream`):** boost seeded 12 firms / 56 submissions / 48 reviews (`boundaryClassified {demo:162, pilot:4}`) → `compute-benchmarks` (exit 0, refreshed BenchmarkRun/CompanyBenchmark cohort) → inspector. **Prod pre-boost vs=68 == local** (bands transferred exactly). **Inspector: BattleCard 25 firms · strong=2 (Δ19.7/18.7) · good=5 · weak=18 · vs 66; Category 3/3 cells clear (n 9/13/8); Demand net +19 (23 in / 4 out).** All three ✅.
  - **Swaps NOT needed on prod** — Demand Signals passed naturally (net +19 in the live 90d window); `boost-vendor-swaps.ts bridgepath` was staged but unnecessary here.
  - Local (`:5433`) parity check was strong=2 (Δ22.0/19.7) · good=6 · weak=17 · vs 66 — prod matches within per-firm variance. Idempotent; 4 infra laws respected; typecheck + eslint clean. DB-only, script not in deployed bundle → **NO redeploy owed.**
  - **Recompute-stable:** a 2nd `compute-benchmarks` + inspector re-run confirmed identical BattleCard/Demand; Category cell scores nudged (Client&Docs 71→69, Workflow 74→72) but all 3 still clear. **CLOSED.**
  - **Mythos authed-render owed:** logged-in render of BOTH boosted vendors (Meridian + Bridgepath) BattleCard/Category/Demand rides the **final regression sweep** (not a separate step).
- **✅ 13c-g COPY — DONE (2026-07-16, commit `4747a3ad`).** typecheck + eslint clean · 103 unit tests green.
  - **13c** Category suppressed cell: "Insufficient peer data" (read as vendor-fault error) → "Field still forming · N vendors" + non-error paragraph. ("firms"→"vendors" was already fixed at HEAD.)
  - **13d** Peer Position empty-state: stopped blaming "completed enough alignment modules" → benchmark-coverage framing (matches the real gate — empty cohort, not the firm's modules).
  - **13e** Membership CTAs plan-aware: ELITE member no longer sees "Upgrade to Pro" / "Most popular" on the Pro card — a tier below the member renders "Included with Elite" (no href, no badge); rank via `MEMBERSHIP_TIER_ORDER`. **Dev-speak:** founder tier-card taglines/features already clean; the flagged "checkout scaffold / webhook / provider" strings are **protected no-live-charge copy** (CLAUDE.md hard rule) → left intact.
  - **13f** Admin "Future sync contract" raw-JSON dump card REMOVED from firm+vendor admin pages and both portal admin-tab embeds; contract build + imports cleaned. (vendor/profile "External data contract" + PortalContent explainer cards are distinct surfaces — left in place; revisit if Cam wants them gone too.)
  - **13g** BattleCard firm filter "All N firms" → "All · N firms" (toggle was already a real lane filter).
  - **Locked-Elite evidence-blame:** consistent subline added in `LockedElitePreview` ("Built on firm-reviewed evidence, not self-report — Elite membership brings it live").
  - **Redeploy owed:** these touch deployed surfaces → the next `--prod` is owed; **bundled at the 13h/i + regression sweep** (not deployed piecemeal).
- **🟢 NEXT (resume here): 13h/i UX** — inline expansion full-width own-row (not in grid column); Elite hub cards expand inline; completed product-assessment cards → current grammar; **+ both riders below** → then full regression + screenshots + the bundled `--prod`.
- **✅ 13h RIDERS — DONE + VERIFIED on running :3005 (2026-07-16, commit `eea57a16`).**
  - **Rider #1 (BattleCard inline expansion):** firm brief was a detached page-bottom panel → now expands INLINE in place (clicked card grows to full-width own-row `md:col-span-2 xl:col-span-3`, brief rendered inside, toggle-closed on re-click); same law as insight face cards. Extracted `BattleCardDetailBody`; leak attrs preserved. **Live proof (demo-vendor-elite/Meridian):** click expands exactly 1 card to 927px (vs ~300px collapsed), `detailInFirst=1`, `inlineBelow=true`, collapses on 2nd click, 0 page errors.
  - **Rider #2 (Ask Pat markdown):** `**bold**`/`*italic*` rendered as literal asterisks → new `lib/patMarkdown.ts` pure XSS-safe tokenizer → `<strong>`/`<em>` in `PatTopBar`; newlines kept. Unit-locked (`tests/pat-markdown.contract.test.ts`, 6 tests). PatTopBar renders live on /firm, 0 TypeErrors. (Live chat bold not exercised — local Anthropic key is zero-balance; parser is deterministic + tested.)
- **13h/i REMAINING:**
  - **✅ Elite hub cards expand inline — CONFIRMED live on :3005** (demo-firm-elite /firm/insights): 4 insight cards, clicking toggles `data-expanded` 0→1 inline, 0 page errors. Already implemented via the shared `InsightSurfaceCardGrid` path (Block 12g/12a); now verified authed.
  - **✅ "Completed product-assessment cards → current grammar" — DONE (2026-07-16, commits `9038680c` + `c74928c7`).** Cam clarified: **card grammar = VISUAL design-law, NOT copy** (see [[feedback_card_grammar_visual]]). The cards were old **text-list** style (title + colon-separated `label: value` rows). Rebuilt on the product-intel catalog card language + swept **all portals** for stragglers per Cam's standing ask ("verify all cards are up to date across all portals, I don't want anything else to slip").
    - **New shared primitives:** `app/components/charts/ScoreBar.tsx` (single band-colored score bar, one lexicon) + `app/components/cards/CardChip.tsx` (`CardChip`/`CardChipRow` pills). Reuse `ScoreLockup`.
    - **Cards rebuilt (7):** vendor product-assessment · firm alignment-assessment (FirmModuleCard) · firm product-assessments · firm admin user-insight · individual user/page · user/profile · user/alignment-assessment. Score cards → ScoreLockup + ScoreBar + chips; status cards → chips. **Data + actions unchanged.**
    - **Verified live on :3005:** vendor completed (18 cards, Latest-score lockup + bar), firm alignment-assessment / product-assessments / admin user-insight all render **0 old colon-rows · 0 TypeErrors**. Individual portal is flag-dark (`PAT_ENABLE_INDIVIDUAL_SURFACES` off) → not renderable in-preview; typecheck/lint clean, shares the verified primitives.
    - typecheck + eslint clean across all 9 files.
- **✅ PRE-DEPLOY VALIDATION CHAIN GREEN (2026-07-16).** `CI=1 PAT_VALIDATE_LAUNCH_SKIP_MAC_MINI=1 pnpm validate:launch` — prisma/db/seeds · lint:test · typecheck · **test:unit 873** · build · release:prelaunch (build + asset-integrity self-host) · promote-known-good all ✅. `test:e2e:local-review` initially 5-red — **all 5 were pre-existing, NOT from 13c-g/13h/13i** (first full validate:launch since 13a; Gate 1 ran pre-13a):
  - 3 stale CONSULTANT specs (`audience-guard`/`alignment-board`/`battlecard`) encoded pre-13a "consultant may view firm/vendor" + cross-tenant-404 — 13a walls consultants to `/consultants` (confirmed benign via error-context: consultant lands on OWN `/consultants`, no leak). **Realigned to the wall.**
  - 2 ADMIN `/admin` timeouts — **PROVEN real/deterministic** (isolated re-run failed identically; error-context showed the admin landing on `/firm`). Root cause: the `review.admin` fixture was FIRM-company-bound, so 13a routes it to `/firm`. **Fix: fixture → company-less platform operator** (13a's model; `/admin` layout requires a company-less operator).
  - **Re-run after fix + re-seed: 27 tests → 26 passed / 1 skipped / 0 failed.** F14 (consultant scoped board access) tracked in Deferred features; `ELITE-REVIEW-PATH.md` realigned to the wall.
- **🟢 NEXT (resume here): bring up `:3005` as the BUILT STANDALONE** (promote-known-good → restart, NOT the dev server — the sweep verifies against built output per the running-server law) → **asset-integrity on `:3005`** → post the review-account list → hand to **Mythos regression sweep** (incl. authed render of both boosted vendors Meridian + Bridgepath, + the 13i modern cards). **After sweep green: bundled `--prod`** (all of 13c-g + 13h + 13i + the e2e/audience fixes owe one redeploy — nothing shipped piecemeal), then Cam's closing pass on patalign.com.
- **Redeploy `a3e8e2af` STANDS** — it bound `PAT_ENABLE_PAT_ASSISTANT`+`PAT_ENABLE_PINGS` (flags are deploy-time; BattleCard data is DB-driven, no redeploy needed for the boost).
- **THEN 13c–g copy** (incl. suppressed-cell copy), **13h/i UX**, regression + screenshots.
- **Infra laws (hard-won):** (1) scripts calling seed/surface helpers MUST use the `@/lib/prisma` singleton — a 2nd `new PrismaClient()` = pool DEADLOCK (cost hours); (2) local validation = FOREGROUND + `[+Ns]` heartbeat, never silent background; assume dead past 10 min; (3) `pkill -9 -f query-engine` between runs (leak → pool exhaustion); (4) never assert against a backgrounded seed/compute (race).
- **Tools (scripts/demo/):** `inspect-vendor-evidence`, `boost-vendor-swaps`, `boost-vendor-firms` (EXPANSION vendors), `boost-vendor-bench-firms` (BENCH catalog vendors — Sentinel/Bridgepath/Lumen/Stratabind; vs-relative lanes), `seed-review-consent`, `assert-prod-demo-expansion`, `report-prod-demo-orphans`, `prod-review-accounts`. Prod runs: `set -a; source .env.prod; set +a; DATABASE_URL="$DIRECT_URL" node --import tsx <script>`.
- **Held:** 9 demo orphans (L6, no deletions) — still awaiting sweep disposition.
- **D0-PROD canonical:** 114 DEMO firms / 24 vendors + boost firms (Meridian +12 → ~126; Bridgepath boost adds +12 firms/+0 vendors once run on prod → ~138). Pilot 4, PRODUCTION 0. **NB: the +12 Bridgepath firms shift `PAT_EXPECT_FIRMS` in `assert-prod-demo-expansion` — the inspector is the boost's acceptance gate, not the count assert.**

## Goal
Founders-preview deploy to **patalign.com** (Cam GO, morning run 2026-07-14). Pulls forward
deploy-night steps 1/3/4 only. **Rotations + Stripe stay July 21.**

**Cam's rule (verbatim):** "every gate below prints proof before the next step runs. No gate, no deploy."

**Deploy shape (Mythos):** cloud-build → gate on deployed **preview** → `vercel --prod` the **same
commit** with production env (NOT `vercel promote` — preview/prod env targets are split; promoting
would carry the preview DATABASE_URL/AUTH_URL onto patalign.com). No protection-settings changes on
the production project. A1/A2 auth proof moves to **post-seed on patalign.com**.

## Current position (2026-07-14T15:11Z)
- **Gated + ship commit:** `4c1d27a0` (branch `feat/agent-system-phase-0`) — the ledger doc is IN this commit, so gated tree == ship tree, no divergence. (Runtime-identical to code commit `c6a5b331`; `4c1d27a0` just adds this doc, which is not traced into functions.)
- **Preview URL (SSO-authed browser):** https://pat-c2acct-live-lkk09k7yu-cams-projects-cbec4d2e.vercel.app
- **Baked fingerprint (this build):** commit=`4c1d27a` buildId=`4c1d27a-mrksgd3f` source=`cloud-build` ts=`2026-07-14T15:10:49Z` → footer Release id should read `4c1d27a:4c1d27a-mrksgd3f`. Vercel metadata gitCommitSha=`4c1d27a` (confirmed).
- **Status:** ✅ **MYTHOS PREVIEW GATE GREEN (2026-07-14T15:17Z)** — footer == `4c1d27a:4c1d27a-mrksgd3f` exact; health JSON commit/timestamp/sourceType all fresh from the single baked file (item 3 closed); `reason=connection_or_auth` (item 2, non-blocking); `/admin` clean branded redirect. **Next action: `vercel --prod` commit `4c1d27a0`, production env.**
- **Tag `founders-preview-2026-07-13`** still points at the pre-fix commit — move to `4c1d27a0` before prod.

## Gate ledger
- **Gate 1 (pre-flight)** ✅ — validate:launch green single-worker (`CI=1 PAT_VALIDATE_LAUNCH_SKIP_MAC_MINI=1`): 820 unit + 26 e2e/1 skip. Stale-test drift fixed behavior-first (insight-elite-stacking, local-review-auth 244/385, data-insight-key). Cross-tenant 404 proven live (E5 security) before any test edit.
- **Gate 2a — TRUE COMMIT** ✅ — Vercel metadata `gitCommitSha=c6a5b33` == HEAD (not self-report).
- **Gate 2b — RELEASE METADATA (Mythos item 3)** ✅ FIXED in `c6a5b331`: single-source-of-truth baked fingerprint. Bake runs BEFORE `next build` (nft traces `lib/release/baked-fingerprint.json`); bake generates buildId, `next.config.generateBuildId` reads it so `.next/BUILD_ID`==baked; `getReleaseFingerprint` reads ALL fields from that one file; `resolveCommitSha` drops the stale `contract.baselineCommit` fallback → cloud build with no bake FAILS LOUD (no more 078a41f/2018 chimera).
- **Gate 2c — DB classification (Mythos item 2)** ✅ **CONFIRMED** — Mythos hit `/api/health/db` at **2026-07-14T15:17:16.575Z** (preview `pat-c2acct-live-lkk09k7yu`); response JSON reported **`reason=connection_or_auth`** (engine loaded; stale **preview-target** DATABASE_URL from the June 9 rotation; prod target is fresh) = **non-blocking**, exactly as expected. The mirrored server log line `[db-health] FAIL reason=connection_or_auth name=… code=… msg=…` is emitted by `app/api/health/db/route.ts` but **not retroactively retrievable** — Vercel retains runtime logs only via a live `vercel logs` tail or a log drain (none configured); the v3 events API returns build events only. The response-JSON `reason` is the authoritative record. (To capture the verbatim line: run `vercel logs <preview-url>` streaming, then hit the route once in an authed browser session — deferred, not deploy-blocking.)
- **Gate 2d — /admin** — accepted by Mythos as clean branded redirect (no ENOENT); full includes proof runs on patalign.com after --prod.

## Prod deploy — DONE (2026-07-14, steps 1–3 green)
- Tag `founders-preview-2026-07-13` moved to `4c1d27a0`; built the detached gated commit; `vercel --prod` (agent env stripped, `PAT_COMMIT_SHA=4c1d27a099…`, cloud-build) → aliased **patalign.com**, ready 3m, exit 0. Bake: `commit=4c1d27a buildId=4c1d27a-mrkt40ai`.
- Prod gates GREEN: `/api/release-fingerprint` 200 `releaseId=4c1d27a:4c1d27a-mrkt40ai` · `/api/health/db` 200 ok:true · Prisma-engine assert PASS · asset-integrity (remote, deployed-buildId-anchored) PASS on `/sign-in`+`/methodology` (10/10 assets 200+typed).

## INCIDENT — prod schema behind deployed app (2026-07-14, during step 4)
- **What:** `demo-expand --apply` and the assertion read both threw `P2022 — column Company.dataBoundary does not exist`. Migration `20260709160000` (dataBoundary + boundary classification) — and likely the whole June→July backlog — **never ran on prod Neon**. The deployed app (`4c1d27a`) expects the current schema; **any authed Company-aggregate surface would 500 at request time** (public/static routes `/`, `/methodology`, `/release` stayed 200 because they don't touch the column; `/platform` `/profiles` 307 auth-gated).
- **Why `/api/health/db` 200 didn't catch it:** health/db is a bare connectivity probe (`SELECT 1`-class), NOT a schema-currency proof. Connectivity ≠ schema parity.
- **PERMANENT RUNBOOK RULE:** the **migration gate moves BEFORE all data steps**. Order is now: `migrate deploy` → re-verify (`/api/health/db` 200 **+ one data-bearing route/Prisma read that touches a migrated column**) → base seed chain → demo-expand. Never seed against an unverified schema again.
- **D0 RULING (Mythos):** `D0-LOCAL = 238/43` is the ACCUMULATED review DB → **local assertion only**. `D0-PROD` = whatever the canonical chain (base + demo-expand) **deterministically** produces → RECORD it here post-apply as prod's canonical demo scale; do NOT chase 238/43, do NOT invent firms. Idempotency proven by re-`--apply` + assertion locked via `PAT_EXPECT_FIRMS/PAT_EXPECT_VENDORS`.
- **Branch determination:** expand dry-run reported `0 demo-expand ecosystems / 0 firms present` → no prior expand; the "partial expand" baseline was a false read (assertion errored on the missing column). → **Branch A** (full base chain then `--apply`), migration-first.
- **Route probe (pre-migration, unauthenticated, 2026-07-14):** `/` 200 · `/methodology` 200 · `/release` 200 · `/api/health/db` 200 · `/platform` 307 · `/profiles` 307 · `/api/fmi` 404. **No unauthenticated route 500'd** — public marketing surface unaffected. The `P2022` exposure is **latent on authed Company-aggregate SSR surfaces** (firm insights, `/vendor/battlecard`, consultant ecosystem, `/admin/organizations`) which need a session to hit; not proven live. NB: a post-migration re-probe of these public routes is NOT a recovery signal (they never broke) — recovery is proven by Phase 0b (`P2022` gone on the `Company.dataBoundary` read) + an authed-surface spot-check.
- **FOLLOW-UP (code, not yet done):** upgrade `app/api/health/db/route.ts` to include a **schema-currency check** (e.g. probe a recently-migrated column / assert `_prisma_migrations` has no pending) so `/api/health/db` fails loud when the served app is ahead of the DB — connectivity-only health is what let this reach `--apply`. Track as a Block item.
- **RESOLUTION (2026-07-14):** `migrate deploy` on prod applied **6 pending migrations clean, no partials** (`20260708130000_add_adaptive_modules`, `20260709160000_add_company_data_boundary`, `20260710000000_add_tenant_soft_delete_and_receipt`, `20260710200000_add_sandbox_swap_event`, `20260711210000_add_notification_ai_generated`, `20260712180000_add_product_maturity`); 41 of 47 were already present (consistent history, no drift). Prod was 6 behind the July-14 app. **Post-migration re-probe was byte-identical to pre-migration** (no public route changed) — confirming the exposure was authed-only and never manifested publicly. Recovery proven at the schema layer by the Phase 0b `Company.dataBoundary` read (next step).

## Orphan disposition (2026-07-14, L6 detection-only — HOLD)
- **9 orphans found** post-3c (report: `scripts/demo/report-prod-demo-orphans.ts`, threshold freshest−1h): all **June 2–3 2026** rows the current plan no longer emits. NOT the brightline/atlas/cobalt cohort (those refreshed in-plan). The orphans:
  - **7 user-less "Demo —" acct firms** (batch): `demo-firm-company-demo-acct-firm-{northway,cedarvale,brightpath,summit,harbor,meridian,apex}` — each subs≈4-5, ecosystem=1, no users. **`…-meridian` = naming collision with July "Meridian" vendor family.**
  - **2 user-attached singletons** (separate disposition): `demo-firm-company` (Demo Firm LLP, 1 user, subs=5) · `demo-vendor-company` (Demo Vendor Inc, 1 user, subs=4, products=4). Identify the attached account (likely June tour login) before any action.
- **Disposition (Mythos):** HOLD through the seed ladder (orphans don't block). Mythos live sweep probes whether any "Demo —" row surfaces on founder paths (peer cohorts, ecosystem pages, vendor lists, BattleCard firm sets). **If they surface** → SOFT-DELETE (migration `20260710000000`, reversible; `scripts/ops/delete-tenant.ts --execute --reason …`), L6 intact, no hard purge. **If not** → stay, documented as inert June-era rows, revisit post-launch. Before ANY delete: check Founders' Manual + Consultant Guide tour paths for named references.
- **Prepared (NOT run) — awaiting sweep greenlight.** Batch-7 dry-run + execute forms staged; 2 user-attached rows need account-identify first.

## Step 3b RESULT — GREEN (2026-07-14)
- Ladder ran migration-first: `migrate deploy` (6 migrations) → 3a baseline → 3b pat-runtime → 3c demo-benchmark (demo:86/pilot:4) → orphan report (9, HELD) → 3d product-maturity (59/60 products, 354 snapshots) → 3e demo-swaps (764 events / 16 vendors) → 3f taxonomy (27 buckets) → 4 expand `--apply` (8 vendors · 34 products · 44 firms; `boundaryClassified {demo:138, pilot:4}`) → 5 assert.
- **D0-PROD CANONICAL SCALE = 114 DEMO firms / 24 DEMO vendors** (70+44 / 16+8 = 138 DEMO companies) · 4 PILOT · **0 PRODUCTION**. Recorded per the D0 ruling (NOT 238/43). Orphan-inclusive: the 9 held orphans are inside the 114/24 and are DEMO-classified.
- Assert GREEN: **ND 0** · **A7** (`demo-* not DEMO: 0` / no PILOT/PROD leak / PRODUCTION 0) · **D5 51 ELITE, all ACTIVE**.
- **First step-5 run FAILed A7=`demo-* not DEMO:20` — measurement race, NOT a defect:** the assert ran concurrent with a backgrounded expand and caught 20/52 new rows in expand's PRODUCTION-default pre-classify window (D0 still read pre-expand 70/16 = proof). Foreground re-run after expand completed → clean. **Runbook rule: never assert against an in-flight seed.**
- **Idempotency lock — ✅ DONE:** re-`--apply` was idempotent (`DB state: 8 ecosystems/44 firms already present`, `linkedFirmUsers: 0`, `demo:138/pilot:4` unchanged); locked assert `firms == 114 (idempotent)` / `vendors == 24 (idempotent)`, A7/ND/D5 all green. **STEP 4 (PROD DEMO DATA) CLOSED.**
- **FOLLOW-UP (Block item, not tonight) — A7 crash-window:** expand (and demo-benchmark) create rows at `dataBoundary=PRODUCTION` default and only classify DEMO at end-of-run; a mid-run crash leaves demo rows PRODUCTION on prod until the next successful seed. Fix = **boundary-at-birth** (create `demo-expand-*`/`demo-bench-*` with `dataBoundary=DEMO` explicitly at the create site; keep end-of-run `classifyCompanyBoundaries` as belt-and-suspenders).

## Step 5 RESULT — flags enabled + redeployed (2026-07-14)
- **Flag diff applied to Production:** `+PAT_ENABLE_ALIGNMENT_BOARD=1` (board + sandbox — same flag), `+PAT_ENABLE_BATTLECARD=1` (vendor BattleCard). Insights + Elite surfaces are **membership-gated (no flag)** — live via the 51 seeded ELITE-ACTIVE accounts (D5). Unchanged: `PAT_ENABLE_SELF_SIGNUP`, `PAT_ENABLE_CONSULTANT_ACCESS`. **`PAT_ENABLE_LOCAL_REVIEW_AUTH` stays ABSENT (OFF)** — launch gate; double-guarded by the loopback-origin check in `lib/auth/localReview.ts` (public `patalign.com` fails it regardless).
- **Why redeploy:** Vercel binds env vars at deploy time — the running deploy wouldn't see new flags. Redeployed the **same gated commit `4c1d27a0`** (stash → checkout → `vercel --prod` → checkout back → pop). New buildId `4c1d27a-mrl0hlb3`, same commit `4c1d27a099…`.
- **Gates GREEN:** `/api/release-fingerprint` 200 `releaseId=4c1d27a:4c1d27a-mrl0hlb3` · `/api/health/db` 200 ok:true · asset-integrity (remote, buildId-anchored) PASS (`/sign-in`+`/methodology`, 10/10 assets).
- **NOT yet verified (needs authed session — Mythos live sweep or Cam):** Alignment Board card on `/firm` + BattleCard on `/vendor` rendering for a demo ELITE account. Config + deploy proven; the logged-in render is not (auth-gated, unreachable unauthenticated).

## Step 6 RESULT — post-deploy proof (2026-07-14, in progress)
- ✅ **asset-integrity** (patalign.com, `4c1d27a-mrl0hlb3`): PASS.
- ✅ **`/api/health/db`**: 200 ok:true.
- ✅ **qa-smoke pin revert**: removed `PAT_QA_EXPECTED_COMMIT=078a41f` from `.env.local` (line 47; backup `.env.local.bak.qa-pin-revert-20260714`) per the 2026-07-07 incident REVERT note — qa-smoke re-baselines the real live commit (`4c1d27a`) on its next scheduled run instead of the chimera pin.
- ✅ **supervisor heartbeat fresh**: `artifacts/agents/supervisor-heartbeat.json` `lastOkPollAt` within ~1s of now, pid 28862 actively polling.
- ⏳ **A1/A2 authenticated spot-proof on 4 demo accounts** — PENDING (needs demo-account sessions; Cam or Mythos live sweep). Same session covers the Step 5 residual: Alignment Board card on `/firm` + BattleCard on `/vendor` render for a demo ELITE account.

## Step 7 REVISED — shared REVIEW accounts (Mythos/Cam, 2026-07-14)
- **FIVE shared logins** (`@c2acct.com`), `mustChangePassword=FALSE`, strong generated passwords, terminal-only. Purpose: founders compare Pro vs Elite side by side; also serve as Mythos's sweep accounts. Personal named founder/consultant seats are POST-review. Script: `scripts/demo/prod-review-accounts.ts` (discovery + `--provision`); **supersedes** prod-tour-accounts.ts (deleted).
  - `firm-pro@c2acct.com` → richest PRO demo-bench firm · `firm-elite@c2acct.com` → richest ELITE demo-bench firm
  - `vendor-pro@c2acct.com` → richest PRO vendor · `vendor-elite@c2acct.com` → Meridian Practice Cloud
  - `consultant@c2acct.com` → owns one rich ecosystem (reassigned; Meridian ecosystem when present)
- **Q1 (concurrent sessions) — YES, safe:** credentials provider ⇒ JWT/stateless sessions ⇒ many browsers on one shared account, no server-side conflict.
- **Q2 (same company both tiers) — NO, TWINS REQUIRED:** membership is company-scoped (`resolveMembershipContext` reads the user's company subject; `MembershipSubscription @@unique(subjectId)` = one tier/company). So Pro & Elite are two different (richest, comparable) companies. Meridian is ELITE ⇒ it's vendor-elite; vendor-pro is a different vendor. Data-parallelism is best-effort (archetype not in DB; twins ranked by submission density).
- **PROVISION NOW** (these are also the sweep accounts). **Sweep verdict gates the emails/personal accounts going OUT — not the provisioning.** Validated end-to-end on local incl. the consultant ecosystem-reassignment.
- **Prod status:** run-1 `--provision` succeeded (all 5 live on prod: firm-pro=Greenfield & Marsh, firm-elite=Calder Pierce & Boon, vendor-pro=Bridgepath Suite, vendor-elite=Meridian Practice Cloud, consultant→Meridian ecosystem). Password generator flake fixed (`randomBytes(base64url)` didn't guarantee the lower+upper+digit policy → now composed with one guaranteed char per class + shuffle; policy-compliant by construction). `upsertLogin` is idempotent — **re-running `--provision` ROTATES all 5 passwords** (upsert overwrites `passwordHash`) and re-prints; use the latest printout. Creds terminal-only.

## BLOCK 13 — founders-preview hardening (Mythos 5-account sweep verdict, 2026-07-14) — NOT GREEN
Sweep bounced. Order: 13a/13b security → 13c-g copy → 13h/i UX → 13j/k data; full regression + screenshots at end. **Gates all outbound (founder emails, launch).**
- **13a ROLE WALL (P0 SECURITY):** vendor portal accepts any authenticated role — consultant creds reached the full vendor workspace ("Vendor company: Unbound"). **Root cause:** `lib/audiencePolicy.ts audienceHomeFor()` returned `null` for `isConsultant` (and ADMIN) → `resolveAudienceRedirectTarget` treats null as "stay" → consultant/admin never walled from `/firm`|`/vendor`. Also `isAdminRole` = ADMIN||OWNER with no company check → company-admins conflated with platform admins.
  - **DONE (route level, tested):** rewrote `audienceHomeFor` — consultant→`/consultants`, **company binding wins over ADMIN role** (firm/vendor OWNER/ADMIN stay in their portal; only a company-LESS operator → `/admin`), unknown-kind→null(stay). `PortalAudienceSegment`+`AUDIENCE_HOME_PATH` extended (consultant, admin). `enforceAudience` now also applied in `/consultants` + `/admin` layouts (was firm/vendor/user only). Closed a parallel hole: firm-OWNER could reach `/admin` and `/vendor`. Contract test `tests/audience-role-wall.contract.test.ts` — **47 passing** (5 profiles × 5 portals both directions + P0 regressions). typecheck clean.
  - **DONE (sign-in defense-in-depth):** `resolveUserAudienceHome` extracted from `enforceAudience`; `/sign-in` redirects an already-authenticated account to its home (no wrong-role-tab lingering).
- **13b PROD SIGN-IN DEV LEAK:** `/sign-in/firm` rendered "Missing env: AUTH_GITHUB_ID/SECRET" + local auth-setup diagnostics publicly. **DONE:** added `authRuntime.diagnosticsVisible` (NODE_ENV!==production); gated the amber missing-env block, "Review local auth setup" link, plumbing panels, prod-clean intro copy (RoleSignInPage) + the hub's local-review-not-ready block. **Verified live on prod:** `/sign-in/firm` HTML has zero auth diagnostics, provisioned-pilot form intact.

### 13a/13b — SECURITY SECTION CHECKPOINT ✅ DEPLOYED (2026-07-14T21:06Z)
- Commits `92618e95` (security) + `bdd48846` (ops/ledger). Gates: typecheck clean · lint clean (`--max-warnings 0`) · **867/867 unit** (incl. 47-test role-wall contract + realigned `audience-guard.contract.test.ts` — the old test encoded the vulnerable admin/consultant bypass).
- Deployed via cloud-build preview (`raabmh701`, SSO-walled) → **`vercel --prod`** → **patalign.com** `releaseId=bdd4884:bdd4884-mrl55plk` commitSha=`bdd48846…` (truthful) · health/db 200 · asset-integrity PASS (10/10) · unauth walls `/vendor`/`/consultants`/`/admin`→307 · 13b HTML-verified.
- **Mythos browser-proof owed:** consultant creds bounced off `/vendor`→`/consultants` on patalign.com (needs auth session — the review accounts). Then 13c-13g.
- **13c-g COPY:** locked-Elite evidence-blame copy; membership CTAs must be plan-aware (no "Upgrade to Pro"/"MOST POPULAR" for Elite) + scrub dev-speak; Peer Position empty-state (benchmark coverage, not modules); admin Future Sync Contract card removal; BattleCard "All · N firms" real toggle.
- **13h/i UX:** inline expansion full-width own-row (not in grid column); Elite hub cards expand inline; completed product-assessment cards → current grammar.
- **13j/k DATA (prod, sourced-secret):** run benchmark computation on prod (Peer Position has no cohort); evidence boost tour hosts — Meridian ≥15 reviewing firms, n≥5 category cells + 3-review divergence floors, swaps in 90d window, firm-elite host one below-bar capability (Gap Plan demo), consultant feed interleave (D4).
- **PASSES banked:** A2 direct-route walls all 4 · board both tiers (B6/C4/A6) · 12d/12g live · honest empty states · branded 404 · E4/E5(firm) · evidence lexicon · footers true.

## Remaining steps (after Mythos green)
1. `git tag -f founders-preview-2026-07-13 4c1d27a0`.
2. **`vercel --prod`** — deploy the **gated commit `4c1d27a0`**, **production** env. If repo HEAD has moved past it (only later docs commits do), `git checkout 4c1d27a0` first so the built tree == gated tree, then: `env -u AI_AGENT -u CLAUDECODE vercel --prod --build-env PAT_COMMIT_SHA=4c1d27a0... PAT_COMMIT_REF=feat/agent-system-phase-0 PAT_BUILD_SOURCE=cloud-build`. Prod footer must read `4c1d27a:…` — same commit Mythos gated on preview.
3. **Gate prod:** `node scripts/release/assert-vercel-prisma-engine.mjs --deployed https://patalign.com` (needs `/api/health/db` 200) · asset-integrity on patalign.com · footer Release id == c6a5b33:… .
4. **Step 3a — MIGRATION GATE (must precede all data):** `DATABASE_URL="$DIRECT_URL" node --import tsx scripts/prisma-safe.ts migrate deploy` (direct/non-pooled — pooler breaks migration advisory locks). Then re-verify: `/api/health/db` 200 **AND** a data-bearing Prisma read (the demo assertion script now reads `Company.dataBoundary` without P2022).
5. **Step 3b — PROD demo data (Branch A):** all on PROD Neon via **DIRECT_URL, serialized**, idempotent. Seeds UPDATE-in-place (confirmed: `ensureCompany.update`, `ensureProduct.update` incl. B10a category canonicalization, membership/submission/firm-maturity upserts) — re-seeding refreshes June-era rows to July-canonical content. Order (Mythos ruling 2026-07-14): **3a** `prisma/seed.ts` → **3b** `scripts/seed-pat-runtime.ts` → **3c** `scripts/seed-demo-benchmark.ts` → **orphan report** (`scripts/demo/report-prod-demo-orphans.ts`, L6 detection-only, HOLD) → **3d** `seed:demo-product-maturity` (table born empty by migration `20260712180000`) → **3e** `seed:demo-swaps` (Demand Signals = headline Elite surface) → **3f** `seed:accounting-taxonomy` (category nav/filters) → **4** `expand-demo-ecosystem.ts --apply` → **5** assert. Assert (per D0 ruling): **record D0-PROD** (canonical, not 238/43), 0 name-dupes, A7, D5 ACTIVE; re-`--apply` + `PAT_EXPECT_FIRMS/VENDORS` lock proves idempotency. D1/D2/D3 + face==detail/elite==pro = Mythos live sweep. Watch Neon P2037. Scripts: `scripts/demo/assert-prod-demo-expansion.ts`, `scripts/demo/report-prod-demo-orphans.ts`.
5. **Step 4 — flags:** enable on prod the exact set :3005 runs (board, BattleCard, insights, sandbox, elite). **Print flag diff.**
6. **Step 5 — post-deploy proof:** patalign.com asset-integrity · qa-smoke pin revert (078a41f → true c6a5b33 fingerprint) · `/api/health/db` 200 · supervisor heartbeat fresh · authenticated A1/A2 spot-proof on 4 demo accounts.
7. **Step 6 — founder accounts:** provision **Randy Johnston, Leslie Garrett, Brian Tankersley** — Elite firm + Elite vendor seats, first-login password change flagged. **Credentials printed to terminal ONLY — never in files or commits.**

## MYTHOS SWEEP (2026-07-16, standalone c959824 → fixes on 6495bb4)
- **PASS:** 13h BattleCard brief expands inline at the clicked row (full-width, radar + module-gaps, verified FIT #10 mid-list) · "All · 25 firms" toggle · Bridgepath 2/5/18 named · 13i assessment cards modernized · Ask Pat markdown bold + sources · 13d CTA logic (Elite → "current plan"/"Included with Elite", inversion dead).
- **BOUNCE → FIXED (13d residual membership copy, commit `6495bb49`):** `/vendor/membership` (+ firm/individual twins) still framed Elite as "staged/future … should not overstate" and used "checkout scaffold" / "Current truthful scope" dev-speak — contradicting live Elite. Reframed Elite as LIVE (built + live for members; Pro sees a locked preview); preserved the no-live-charge truth in plain words; renamed the `staged` identifiers (incl. `MembershipSurfaceGate` prop across 18 callers). **ENFORCED:** `banned-vocabulary.contract.test.ts` bans "checkout scaffold"/"truthful scope"/"staged" on the 7 membership surfaces. Verified on `:3005` (6495bb4): both membership pages 0 banned phrases · Elite-live copy · no-charge intact · Elite-viewer CTA has no "Most popular"/"Upgrade to Pro" · 0 TypeErrors. 874 unit green.
- **NEXT: bundled `--prod`** of HEAD `6495bb49` (all of 13c-g + 13h + 13i + e2e/audience fixes + boosts + this 13d fix — one deploy). Then Cam's closing pass on patalign.com (verifies membership copy + everything). Promoted known-good local = `6495bb4:DSauvsbXkOkYIa13rtexj`; `:3005` built standalone up on it.

## Deferred features
- **F14 — consultant scoped read-only board access (post-launch).** A consultant
  should be able to open the alignment board / BattleCard of firms/vendors **in
  their assigned ecosystems** (read-only). Cam's ruling (2026-07-16): this is a
  **deferred FEATURE, not a 13a regression** — implement as a **proper scoped
  authorization check** (verify the target firm/vendor is in the consultant's
  assigned ecosystems), **never a route exemption / `?firm=` bypass**. Until then
  the 13a role wall stands: consultants are walled to `/consultants` and review via
  `/consultants/ecosystems/<id>`. E2e specs (`audience-guard`, `alignment-board`,
  `battlecard` consultant cases) + `ELITE-REVIEW-PATH.md` were realigned to the wall.

## Deferred / notes
- **:3005 UP as the BUILT STANDALONE (2026-07-16)** — promoted known-good `54f5e71:b5J1tM-P5i6mMTEPRi9X1` (HEAD), served via `node .next/standalone/server.js` PORT=3005 + all review flags; asset-integrity PASS (served==disk==fingerprint, /sign-in+/methodology 10/10). Preview DB restored (demo-expand → compute → swaps → preview-pat-setup) + both boosted vendors re-applied locally (Meridian 18 firms 5/4/9, Bridgepath 25 firms 2/5/18). Ready for the Mythos sweep. (:3000 launchd app not restarted — SKIP_MAC_MINI.)
- e2e local-review must run single-worker (`CI=1`) or the stateful consultant test flakes.
- `baked-fingerprint.json` is gitignored (per-build artifact).
