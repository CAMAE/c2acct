# Cam's Board Redlines — Round 2 (Alignment Sandbox) · July 8
### Verdict on Group 3 v1: right skeleton (swap flow + projected banner approved), wrong skin. Rebuild the visual layer per below before Group 4, since the BattleCard inherits this visual language.

## R11.2 — Real puzzle-piece shapes
Pieces must be drawn as actual puzzle pieces — a clean single-outline jigsaw silhouette (knobs/notches on the sides), NOT rounded cards with interlock hints. Make pieces large enough to hold their info comfortably (name, vendor line, score chip). SVG path pieces sized ~180-220px are fine; two interlocking rows (stack row interlocked together; candidate row below).

## R11.3 — Color system (match what exists)
Current board is "too blue." Use the established palette roles: stack pieces = light/near-white fill with the standard card border, matching the pill-toggle-menu treatment; SELECTED stack piece = highlighted outline (brand orange or c2-blue ring, consistent with existing selection states); candidate/Secret pieces = c2-blue (#063674) fill with light text. Semantic delta colors stay green/orange as shipped.

## R13.2 — Realistic stack + richer candidate pool
The reviewed firm must run at least 4 products in its stack (5 like demo-firm-elite is ideal), and the candidate rail should offer ~12 Secret Products to play with. Critical data fix: BOTH current candidates project negative — the sandbox must include candidates with HIGHER projected alignment (draw from the expanded demo vendors' stronger products) so the game is winnable. Mix: majority positive-to-neutral, a few negative for realism.

## R11.4 — Secret Product ranking
Rank the candidate rail like the BattleCard: "Sandbox Fit #1, #2, #3…" ordered by projected alignment gain, best first. The tease is "the #1 piece for your firm exists — Reveal with Elite."

## R12.2 — Radar projection (the big add)
Add the two-polygon positioning radar (same component family as the vendor-brief FiveModuleRadar) at the top of the Sandbox, five module axes: solid polygon = current alignment shape; second polygon = PROJECTED shape, redrawing live on each swap. Same legend/color language as the consultant brief (current = blue, projected = orange). This plus the existing per-piece breakdown bars = the "what data actually changes" detail Cam asked for; keep both.

## R11.5 — Layout + naming
Title the surface "Sandbox" (eyebrow: ALIGNMENT SANDBOX) at top, consultant-brief visual standard throughout: stat lockup banner (current vs projected), radar, stack row, candidate rail, breakdown panel. The firm-portal card entry (already added) keeps the Sandbox name.

## Order
Rebuild board per above → screenshot checkpoint for Cam (both Pro/Elite states side-by-side again) → then Group 4 BattleCard (inheriting this visual language: same piece/radar/stat-lockup standards) → Group 5 (R17 + Blocks G+H). Full chain + ledger per group, as always.
