# Accounting eval set (V1 scaffold)

Gold scenarios for regression-testing the accounting Vertical Pack. Per
Blueprint §6 move (e), the eval harness should exist before the second vertical.
Phase 2 ships the structure; scenarios are added as accounting behavior is
exercised. The per-agent eval harnesses (`pnpm agent:eval:<key>`) remain the
runtime-level checks.
